import React, { useContext } from "react";
import { Context } from "../main";
const Login = () => {
  const [isAuthenticated, setIsAuthe] = useContext(Context);
  return <div></div>;
};
export default Login;
