import React, { useContext } from "react";
import { Context } from "../main";
const Login = () => {
  const [isAuthenticated, setIs] = useContext(Context);
  return <div></div>;
};
export default Login;
